# Core
openai>=1.50.0
httpx>=0.27.0
aiohttp>=3.9.0
click>=8.1.7
rich>=13.7.0
python-dotenv>=1.0.0
psutil>=5.9.0
pyyaml>=6.0

# WhatsApp — native Python client (whatsmeow via Go binary)
neonize>=0.2.1

# Interactive CLI - Rich-based menus
questionary>=1.10.0

# Skills
duckduckgo-search>=6.1.0

# Vector memory (sqlite-vec extension for semantic search)
sqlite-vec>=0.1.6

# Testing (for E2E tests)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

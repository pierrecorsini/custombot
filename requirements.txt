# Core (pinned for reproducible builds)
openai~=2.29.0
httpx~=0.28.0
aiohttp~=3.13.0
click~=8.3.0
rich~=14.3.0
python-dotenv~=1.2.0
psutil~=6.0.0
pyyaml~=6.0.0

# WhatsApp — native Python client (whatsmeow via Go binary)
neonize~=0.3.16

# Skills
duckduckgo-search~=8.1.0

# Vector memory (sqlite-vec extension for semantic search)
sqlite-vec~=0.1.7

# Fast JSON serialization (hot-path acceleration)
orjson~=3.10.0

# Binary serialization for tool-call result payloads
msgpack~=1.1.0

# OpenTelemetry tracing (optional — graceful degradation if not installed)
opentelemetry-api~=1.30.0
opentelemetry-sdk~=1.30.0

# File system watching for routing rule auto-reload (inotify/ReadDirectoryChanges)
watchdog>=4.0.0

# Fast non-cryptographic hashing for embedding cache keys
xxhash~=3.5.0

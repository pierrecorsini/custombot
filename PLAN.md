# PLAN.md — Improvement Backlog

Auto-generated by codebase review on 2026-05-04.

---

## Architecture & Refactoring

- [x] Split `_bot.py` (1280 lines) into focused sub-modules — extract context-building logic (`_build_turn_context`), response delivery (`_send_to_chat`), and per-chat lock management into dedicated modules under `src/bot/`, following the pattern already used for `preflight.py`, `crash_recovery.py`, and `react_loop.py`
- [x] Decompose `scheduler.py` (941 lines) into `scheduler/engine.py` (tick loop, heap scheduling), `scheduler/persistence.py` (JSONL read/write, atomic save), and `scheduler/cron.py` (UTC conversion, weekday matching) — mirrors the successful `db/` and `config/` package decomposition
- [x] Extract remaining concerns from `message_queue.py` (638 lines) — buffer management and background flush loop into `message_queue_buffer.py`, keeping `message_queue.py` as the thin public API (persistence is already extracted into `message_queue_persistence.py`)
- [x] Consolidate duplicate `chat_id` validation — `src/channels/base.py` (`_validate_chat_id`) and `src/db/__init__.py` (`_validate_chat_id`) have independent implementations with divergent regex patterns and error messages; unify into `src/utils/validation.py` and import from both consumers
- [x] Replace mutable context bags (`BuilderContext`, `StartupContext`) with a lightweight protocol-based DI registry — both use `field: X | None = None` patterns with manual validation; a typed registry would reduce coupling and surface missing dependencies at construction time instead of runtime
- [x] Extract `ErrorHandlerMiddleware._send_error_reply` send→dedup→event pattern into a shared `send_and_track(chat_id, text)` helper on `BaseChannel` — the same 3-step sequence is duplicated in `Bot._send_to_chat`, `ErrorHandlerMiddleware`, and scheduled task delivery

## Performance Optimization

- [x] Cache parsed YAML frontmatter in routing engine keyed by `(filename, mtime, size)` — routing reloads re-parse all `.md` files even when unchanged; a content-addressed cache would eliminate redundant YAML parsing during hot-reload
- [x] Batch recovered messages during crash recovery — `message_queue.py` processes recovered messages one-by-one through the pipeline; grouping them into batches (up to `max_concurrent_messages`) would reduce event-loop scheduling overhead and speed up recovery after long downtimes
- [x] Add managed SQLite connection pooling — the DB layer creates ad-hoc connections in some read paths; a bounded pool (mirroring `FileHandlePool` for file handles) would reduce connection setup overhead under concurrent load
- [x] Wrap vector memory batch inserts in explicit SQLite transactions — `src/vector_memory/__init__.py` `_insert_entries()` now wraps multi-insert operations in `BEGIN IMMEDIATE / COMMIT` with proper rollback on error, reducing fsync overhead by 10–100x for bulk operations
- [x] Replace list-concatenation with `list.extend()` in the ReAct loop hot path — `react_loop.py` creates new message lists via `messages + [new_msg]` on each iteration; in-place `extend()` avoids allocations during long multi-tool conversations
- [x] Evaluate switching `TokenUsage._per_chat` from dict to `BoundedOrderedDict` with configurable cap — the current per-chat tracking dict grows unbounded; an LRU-evicted bounded dict would cap memory usage in high-volume deployments

## Error Handling & Resilience

- [x] Implement per-category retry policies in `app.py` main loop — `_classify_main_loop_error()` already categorizes errors (LLM transient, channel disconnect, filesystem, config), but all use the same recovery path; LLM transient should use exponential backoff, channel disconnects should use fixed-interval retry, and filesystem errors should fail fast
- [x] Add active LLM circuit-breaker recovery — when the circuit opens, the current passive health probe waits for the cooldown timer; add an active probe that sends lightweight completions to detect provider recovery sooner, reducing mean-time-to-recovery
- [x] Implement graceful degradation for vector memory failures — when `sqlite-vec` is unavailable or the vector DB is corrupted, fall back to text-based memory search (regex/grep over `MEMORY.md`) rather than propagating errors that block message processing
- [ ] Add atomic writes (write-to-temp → `os.replace()`) for message queue persistence — the current JSONL append can lose the last line on crash; a WAL-style pattern guarantees no message loss on abrupt termination
- [ ] Add configurable wall-clock timeout for the full ReAct loop — a single stuck LLM call or infinite tool loop can block the event loop indefinitely; propagate a deadline through `ReactIterationContext` and check it between iterations

## Test Coverage & Quality

- [ ] Increase test coverage floor from 75% to 80% — prioritize coverage for: `scheduler.py` (tick loop, cron calc, persistence), `message_queue.py` (buffer flush, recovery), `llm/_client.py` (circuit breaker, streaming), `channels/neonize_backend.py`, and `security/` modules (path validation, prompt injection detection)
- [ ] Add Hypothesis property-based tests for routing engine — cover edge cases in: regex compilation and matching, priority-merge correctness, cache-key collision resistance, and stale-check debouncing that manual tests miss
- [ ] Add integration test for config hot-reload end-to-end — verify that modifying `config.json` at runtime propagates changes to LLM client (model, base_url), channel settings, and rate limiter without restart; test via `ConfigWatcher` + `ConfigChangeApplier`
- [ ] Add end-to-end crash recovery pipeline test — enqueue messages → SIGKILL → restart → verify all messages are recovered and processed in order; currently only unit tests exist for individual recovery components
- [ ] Create contract test suite for `BaseChannel` subclasses — parameterized tests covering: send/receive, safe-mode confirmation, typing indicator, shutdown ordering, and media sending; ensures any new channel (Telegram, Discord) inherits correctness
- [ ] Add mutation testing to CI (non-blocking) — integrate `mutmut` or `pitest` to measure test effectiveness; start with critical modules (`routing.py`, `react_loop.py`, `memory.py`)

## Security Hardening

- [ ] Add HTTP-level rate limiting to `LLMClient` — currently rate limiting exists only at the skill level; add a sliding-window rate limiter in `_client.py` to prevent runaway tool loops from burning API credits when the circuit breaker hasn't opened yet
- [ ] Implement configurable skill sandboxing with resource limits — add to the `shell` skill: max execution time (separate from skill timeout), max stdout/stderr size, allowed-commands whitelist/denylist, and working-directory restrictions beyond workspace isolation
- [ ] Make HMAC signature verification mandatory for scheduled task execution — the scheduler signs prompts on disk but verification is not enforced in all code paths; ensure `process_scheduled` always verifies before executing, preventing prompt injection via file manipulation
- [ ] Apply `filter_response_content()` consistently to all LLM response paths — the filter exists in `security/prompt_injection.py` but is not applied in the ReAct loop's intermediate tool responses or streaming callback; extend coverage to all outbound text
- [ ] Add audit logging for config changes — `ConfigWatcher` detects file changes but doesn't log what changed or who triggered it; add structured audit entries (old value → new value) for security-sensitive fields (api_key, allowed_numbers, base_url)

## Observability & Monitoring

- [ ] Add token cost estimation to `TokenUsage` and health endpoint — map model names to per-token prices (GPT-4o, GPT-4o-mini, etc.) and expose `estimated_cost_usd` alongside existing token counts; display in health check output and structured metrics
- [ ] Implement full OpenTelemetry metrics instruments — replace ad-hoc `PerformanceMetrics` counters with proper OTel `Counter` and `Histogram` instruments for: message latency distribution, LLM call duration, queue depth over time, and per-skill execution time
- [ ] Complete distributed tracing correlation across the full message lifecycle — ensure every span (receive → route → context assemble → react loop → tool exec → send response) carries the correlation ID and has correct parent-child links; currently some mid-pipeline spans lose the parent reference
- [ ] Add structured alerting thresholds to health check — the health endpoint reports component status but doesn't alert on degradation trends; add configurable thresholds (e.g., error rate > 5% in last 5 min, queue depth > 100, LLM p99 > 30s) and expose them as health check sub-statuses

## Developer Experience & Code Hygiene

- [ ] Incrementally fix Ruff `PLC0415` violations (618 total, import-outside-top-level) — start with `src/core/` and `src/bot/` modules; move TYPE_CHECKING-guarded imports to true top-level where possible, and document intentional deferred imports
- [ ] Incrementally fix Ruff `PLR2004` violations (549 total, magic-value-comparison) — extract numeric literals into named constants in `src/constants/` modules; start with `src/bot/`, `src/llm/`, and `src/core/`
- [ ] Enable strict mypy for `src/core/` and `src/llm/` — follow the same non-blocking → blocking promotion pattern used for `src/bot/`; these are the next most critical modules after the bot orchestrator
- [ ] Add `make lint-fix` and `make typecheck-strict` Makefile targets — `lint-fix` runs `ruff check --fix && ruff format` for easy auto-fixing; `typecheck-strict` runs `mypy --strict src/` locally
- [ ] Add `make test-coverage` target that generates an HTML coverage report — `open htmlcov/index.html` for local development; currently only terminal and XML reports are configured
- [ ] Reduce `PLR0913` violations (63 total, too-many-arguments) — extract related parameters into typed dataclasses (e.g., `ToolExecutionParams`, `MessageSendOptions`) following the `ShutdownContext` / `ReactIterationContext` pattern
